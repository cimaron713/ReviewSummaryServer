server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  redis:
    host: localhost
    port: 6379
  profiles:
    include: secret



